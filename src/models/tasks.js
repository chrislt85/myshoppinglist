class Task {
    constructor(id, value, done, imageurl) {
      this.id = id.toString();
      this.value = value;
      this.done = done;
      this.imageurl = imageurl;    }
  }
  
  export default Task; 